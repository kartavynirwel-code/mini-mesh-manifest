apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service-pod
spec:
  selector:
    app: user-service-pod
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP